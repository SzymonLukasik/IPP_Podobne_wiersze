#ifndef PARSING_H
#define PARSING_H

/* Jeśli jest to możliwe, funkcja dokonuje konwersji
słowa [word] w liczbę odpowiedniego typu i zwraca true,
a w przeciwnym przypadku zwraca false. */
bool word_to_number(string word, number* n);

#endif /* PARSING_H */